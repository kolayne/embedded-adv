# Embedded-adv

Code and documentation that are a result of my experiments with embedded devices.

## How to use

Refer to `README.md`s in subdirectories. Remember to create files that are
gitignored and have a `.example` file instead. E.g., if a directory
contains `config.hpp.example`, copy it to `config.hpp` and modify it to your liking.
